<div role="tablist" class="tabs tabs-border">
  <a
    role="tab"
    href="{{ path('app_channel_quizzes', { 'id': channel.id }) }}"
    class="tab{% if app.request.attributes.get('_route') == 'app_channel_quizzes' %} tab-active{% endif %}"
  >
    Подключенные
  </a>
  <a
    role="tab"
    href="{{ path('app_channel_quizzes_all', { 'id': channel.id }) }}"
    class="tab{% if app.request.attributes.get('_route') == 'app_channel_quizzes_all' %} tab-active{% endif %}"
  >
    Все
  </a>
</div>
