<div role="tablist" class="tabs tabs-border">
  <a
    role="tab"
    href="{{ path('app_quiz_users', { 'id': quiz.id }) }}"
    class="tab{% if app.request.attributes.get('_route') == 'app_quiz_users' %} tab-active{% endif %}"
  >
    Подключенные
  </a>
  <a
    role="tab"
    href="{{ path('app_quiz_users_all', { 'id': quiz.id }) }}"
    class="tab{% if app.request.attributes.get('_route') == 'app_quiz_users_all' %} tab-active{% endif %}"
  >
    Все
  </a>
</div>
