<div role="tablist" class="tabs tabs-border">
  <a
    role="tab"
    href="{{ path('app_quality_channels', { 'id': quality.id }) }}"
    class="tab{% if app.request.attributes.get('_route') == 'app_quality_channels' %} tab-active{% endif %}"
  >
    Подключенные
  </a>
  <a
    role="tab"
    href="{{ path('app_quality_channels_all', { 'id': quality.id }) }}"
    class="tab{% if app.request.attributes.get('_route') == 'app_quality_channels_all' %} tab-active{% endif %}"
  >
    Все
  </a>
</div>
